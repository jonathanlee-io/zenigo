import {TimeAgoDatePipe} from './time-ago-date.pipe';

describe('TimeAgoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
